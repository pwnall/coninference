<%= form_for @device do |f| %>
  <table class="device-form">
    <tr>
      <th>Name</th>
      <th>Room</th>
    </tr>

    <tr>
      <td>
        <%= f.text_field :name, placeholder: 'Intel Edison #1', autofocus: true %>
      </td>
      <td>
        <%= f.collection_select :room_id, Room.all, :id, :name,
              include_blank: true %>
      </td>
      <td class="actions">
        <%= f.button type: :submit do %>
          <%= fa_icon :save %> Save
        <% end %>
      </td>
    </tr>
  </table>
<% end %>
