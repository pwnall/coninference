<h2>Room <%= @room.name %> on <%= @room.map.name %><h2>

<% SensorEdge::KINDS.each do |kind| %>
  <h3><%= kind %></h3>

  <%= content_tag :canvas, '', class: 'sensor-data',
        data: { sensor_url: room_sensor_url(@room, kind, format: :json) } %>
<% end %>
